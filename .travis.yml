language: r
r:
  - devel
  - release
cache: packages

r-github-packages:
  - jimhester/covr

after_success:
  - tar -C .. -xf $PKG_TARBALL
  - Rscript -e 'covr::coveralls(line_exclusions= c("src/init.cpp"))'
